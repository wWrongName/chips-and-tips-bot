{
  "name": "chips-and-tips-bot",
  "module": "index.ts",
  "type": "module",
  "engines": {
    "bun": ">=1.0.30"
  },
  "scripts": {
    "format": "prettier --config .prettierrc './**/*.{js,ts}' --write",
    "dev": "bun --watch index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3"
  }
}
